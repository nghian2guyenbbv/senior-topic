spring:
  application:
    name: oauthorization
  security:
    oauth2:
      authorizationserver:
        issuer: http://localhost:9000
server:
  port: 9000
logging:
  level:
    org.springframework.security: TRACE